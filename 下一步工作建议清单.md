# 下一步工作建议清单（按优先级）

## 🔴 P0级（本周必须完成）

### 1. 数据库升舱 - 0.5h

**SQL脚本**:
```sql
-- 添加summary_ai字段
ALTER TABLE entries ADD COLUMN IF NOT EXISTS summary_ai text;

-- 添加索引
CREATE INDEX IF NOT EXISTS idx_entries_scene_tags ON entries USING GIN (scene_tags);
CREATE INDEX IF NOT EXISTS idx_entries_project_code ON entries (project_code, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_entries_section_latest ON entries (section_id, is_latest);
```

**验证方法**:
```bash
sudo -u postgres psql -d rag_db -c "\d entries"
```

---

### 2. VectorClient.search_entries修改 - 1h

**文件**: `ai_factory/agents/memory/vector_client.py`

**位置**: 第62-68行

**修改前**:
```python
sql = f"""
    SELECT
        e.entry_id,
        e.content,
        e.entry_type,
        e.section_id,
        e.created_at,
        1 - (emb.embedding <=> %s::vector) AS similarity
    FROM entries e
    LEFT JOIN entry_embeddings emb ON e.entry_id = emb.entry_id
    WHERE {where_clause}
"""
```

**修改后**:
```python
sql = f"""
    SELECT
        e.entry_id,
        e.title,
        e.summary_ai,
        e.content,
        e.entry_type,
        e.section_id,
        e.created_at,
        1 - (emb.embedding <=> %s::vector) AS similarity
    FROM entries e
    LEFT JOIN entry_embeddings emb ON e.entry_id = emb.entry_id
    WHERE {where_clause}
"""
```

**返回结果修改**（第82-91行）:
```python
results = [
    {
        "entry_id": row[0],
        "title": row[1],         # ✅ 新增
        "summary_ai": row[2],    # ✅ 新增
        "content": row[3],
        "entry_type": row[4],
        "section_id": row[5],
        "created_at": row[6].isoformat() if row[6] else None,
        "similarity": float(row[7]) if row[7] is not None else None,
    }
    for row in rows
]
```

---

### 3. 向量化逻辑修正 - 2h

**说明**: 确保向量化使用`title + summary_ai`，而非`content`

**检查脚本**:
```bash
cd /root/ai-factory
grep -n "def _build_embedding_text" *.py
```

**确认以下脚本使用`title + summary_ai`**:
- `vectorize_entries_with_ollama.py` ✅
- `refresh_titles_summaries_and_embeddings.py` ✅

**如果发现向量化`content`的脚本，修改为**:
```python
def _build_embedding_text(entry: Dict[str, Any]) -> str:
    """构建用于向量化的文本（Knowledge Node Level 1 + Level 2）"""
    title = (entry.get("title") or "").strip()
    summary_ai = (entry.get("summary_ai") or "").strip()

    parts = []
    if title:
        parts.append(title)
    if summary_ai:
        parts.append(summary_ai)

    # 只使用title和summary_ai构建向量文本
    return "\n".join(parts)
```

**重新生成向量**:
```bash
python3 refresh_titles_summaries_and_embeddings.py
```

---

### 4. 实现HybridSearchService - 4h

**新建文件**: `ai_factory/agents/memory/hybrid_search_service.py`

**代码框架**（参考审核报告第3.1节）:

```python
class HybridSearchService:
    """混合搜索服务：先过滤后搜索"""

    def __init__(self, entry_service: EntryService, vector_client: VectorClient):
        self.entry_service = entry_service
        self.vector_client = vector_client

    def search(
        self,
        query_text: str,
        query_embedding: List[float],
        metadata_filters: Dict[str, Any],
        top_k: int = 10,
        threshold: Optional[float] = None,
        rerank_by_importance: bool = True
    ) -> List[Dict[str, Any]]:
        """混合搜索：先过滤后搜索"""

        # 第一步：Metadata预过滤（Level 4 - 硬约束层）
        filtered_entry_ids = self._filter_by_metadata(metadata_filters)

        # 第二步：在过滤后的结果中做向量搜索（Level 1 + Level 2）
        vector_results = self.vector_client.search_entries(
            query_embedding=query_embedding,
            filters={"entry_ids": filtered_entry_ids},  # ✅ 支持entry_ids过滤
            top_k=top_k,
            threshold=threshold
        )

        # 第三步：根据importance_score后置加权
        if rerank_by_importance:
            vector_results = self._rerank_by_importance(vector_results)

        return vector_results

    def _filter_by_metadata(self, filters: Dict[str, Any]) -> List[str]:
        """通过Metadata过滤（SQL WHERE条件）"""
        # ... (见审核报告)
        pass

    def _rerank_by_importance(self, results: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """根据importance_score重新排序"""
        # ... (见审核报告)
        pass
```

**修改VectorClient.search_entries** - 支持`entry_ids`过滤:

```python
def search_entries(
    self,
    query_embedding: List[float],
    filters: Optional[Dict[str, Any]] = None,
    top_k: int = 10,
    threshold: Optional[float] = None
) -> List[Dict[str, Any]]:
    """在 entries 表中进行向量检索。"""
    with connection_scope() as conn:
        with conn.cursor() as cur:
            conditions = []
            params = []

            if filters:
                # ✅ 新增：entry_ids 过滤
                if "entry_ids" in filters and filters["entry_ids"]:
                    placeholders = ', '.join(['%s'] * len(filters["entry_ids"]))
                    conditions.append(f"e.entry_id IN ({placeholders})")
                    params.extend(filters["entry_ids"])

                # section_id 过滤
                if "section_id" in filters:
                    conditions.append("e.section_id = %s")
                    params.append(filters["section_id"])

                # entry_type 过滤
                if "entry_type" in filters:
                    conditions.append("e.entry_type = %s")
                    params.append(filters["entry_type"])

            where_clause = " AND ".join(conditions) if conditions else "TRUE"

            # ... 其余代码保持不变
```

**集成到MemoryService** - 在`get_context_for_turn`中使用HybridSearchService:

```python
# memory_service.py
from .hybrid_search_service import HybridSearchService

class MemoryService:
    def __init__(
        self,
        session_service: SessionService,
        section_service: SectionService,
        entry_service: EntryService,
        memory0_service: Memory0Service,
        model: str = "gpt-3.5-turbo"
    ) -> None:
        # ...
        self.hybrid_search_service = HybridSearchService(
            entry_service=entry_service,
            vector_client=self.vector_client
        )

    def get_context_for_turn(
        self,
        session_id: str,
        agent_id: str,
        max_tokens: int = DEFAULT_MAX_TOKENS,
        rag_top_k: int = DEFAULT_RAG_TOP_K,
        recent_messages_limit: int = DEFAULT_RECENT_MESSAGES
    ) -> ContextForTurn:
        # ...
        # 3. 获取长期记忆（从entries大库）- 使用混合搜索
        rag_snippets = []
        if rag_top_k > 0:
            query_text = self.query_extractor.extract_from_messages(recent_messages)
            if query_text:
                try:
                    query_embedding = self.llm_client.generate_embedding_sync(query_text)

                    # ✅ 使用混合搜索
                    rag_results = self.hybrid_search_service.search(
                        query_text=query_text,
                        query_embedding=query_embedding,
                        metadata_filters={
                            "agent_id": agent_id,
                            "project_code": "project_A"  # 可根据实际情况调整
                        },
                        top_k=rag_top_k,
                        rerank_by_importance=True
                    )

                    # 转换为字典列表
                    rag_snippets = [
                        {
                            "entry_id": result.get("entry_id"),
                            "title": result.get("title"),
                            "summary_ai": result.get("summary_ai"),  # ✅ 添加summary_ai
                            "content": result.get("content"),
                            "similarity": result.get("similarity"),
                            "final_score": result.get("final_score"),  # ✅ 添加final_score
                            "scene_tags": result.get("scene_tags"),
                        }
                        for result in rag_results
                    ]
                except Exception as e:
                    print(f"[MemoryService] Warning: Failed to retrieve RAG snippets: {e}")
        # ...
```

---

## 🟠 P1级（本月内完成）

### 5. EntryService.update_entry修改 - 0.5h

**文件**: `ai_factory/agents/memory/entry_service.py`

**位置1**: 第131行

**修改前**:
```python
if key in ["title", "content", "space_type", "section_id", "agent_id", "source_session_id"]:
    updates.append(f"{key} = %s")
    params.append(value)
```

**修改后**:
```python
if key in ["title", "summary_ai", "content", "space_type", "section_id", "agent_id", "source_session_id"]:
    updates.append(f"{key} = %s")
    params.append(value)
```

**位置2**: 第333行（batch_update_entries）

**修改前**:
```python
if key in ["title", "content", "space_type", "section_id", "agent_id", "source_session_id"]:
```

**修改后**:
```python
if key in ["title", "summary_ai", "content", "space_type", "section_id", "agent_id", "source_session_id"]:
```

---

### 6. Memory0Service添加summary_ai支持 - 2h

**文件**: `ai_factory/agents/memory/memory0_service.py`

#### 6.1 新增_generate_summary方法

**位置**: 在`_detect_conflict`方法之后添加

```python
def _generate_summary(self, content: str) -> str:
    """生成内容的AI摘要（Knowledge Node Level 2）

    Args:
        content: 原始内容

    Returns:
        str: AI生成的摘要
    """
    try:
        prompt = f"""请对以下内容生成简洁的摘要（不超过200字）：

{content}

要求：
1. 提取核心信息和关键点
2. 语言简洁明了
3. 保留重要细节
"""

        summary = self.llm_client.generate_text_sync(prompt)
        return summary.strip() if summary else ""
    except Exception as e:
        print(f"[Memory0Service] Failed to generate summary: {e}")
        return ""
```

#### 6.2 修改_handle_new方法

**位置**: 第247-257行

**修改前**:
```python
entry_id = self.entry_service.create_entry({
    "entry_id": f"ent_{uuid.uuid4().hex}",
    "title": candidate.metadata.get("title", "Memory Entry"),
    "content": candidate.content,
    "scene_tags": candidate.scene_tags,
    "agent_id": candidate.agent_id,
    "space_type": candidate.space_type,
    "importance": 1.0,
    "usage_count": 0,
    "last_seen_at": datetime.now(),
})
```

**修改后**:
```python
# 生成summary_ai
summary_ai = self._generate_summary(candidate.content)

entry_id = self.entry_service.create_entry({
    "entry_id": f"ent_{uuid.uuid4().hex}",
    "title": candidate.metadata.get("title", "Memory Entry"),
    "summary_ai": summary_ai,  # ✅ 添加summary_ai
    "content": candidate.content,
    "scene_tags": candidate.scene_tags,
    "agent_id": candidate.agent_id,
    "space_type": candidate.space_type,
    "importance": 1.0,
    "usage_count": 0,
    "last_seen_at": datetime.now(),
})
```

#### 6.3 修改_handle_override方法

**位置**: 第401-412行

**修改前**:
```python
new_entry_id = self.entry_service.create_entry({
    "entry_id": f"ent_{uuid.uuid4().hex}",
    "title": candidate.metadata.get("title", "Memory Entry"),
    "content": candidate.content,
    "scene_tags": candidate.scene_tags,
    "agent_id": candidate.agent_id,
    "space_type": candidate.space_type,
    "importance": 1.5,
    "usage_count": 0,
    "last_seen_at": datetime.now(),
    "overridden_entry_ids": [existing_entry_id],
})
```

**修改后**:
```python
# 生成summary_ai
summary_ai = self._generate_summary(candidate.content)

new_entry_id = self.entry_service.create_entry({
    "entry_id": f"ent_{uuid.uuid4().hex}",
    "title": candidate.metadata.get("title", "Memory Entry"),
    "summary_ai": summary_ai,  # ✅ 添加summary_ai
    "content": candidate.content,
    "scene_tags": candidate.scene_tags,
    "agent_id": candidate.agent_id,
    "space_type": candidate.space_type,
    "importance": 1.5,
    "usage_count": 0,
    "last_seen_at": datetime.now(),
    "overridden_entry_ids": [existing_entry_id],
})
```

---

## 🟡 P2级（有时间再做）

### 7. MemoryService.rag_snippets返回summary_ai - 0.5h

**文件**: `ai_factory/agents/memory/memory_service.py`

**位置**: 第374-380行

**修改前**:
```python
rag_snippets = [
    {
        "entry_id": result.get("entry_id"),
        "title": result.get("title"),
        "content": result.get("content"),
        "similarity": result.get("similarity"),
        "scene_tags": result.get("scene_tags"),
    }
    for result in rag_results
]
```

**修改后**:
```python
rag_snippets = [
    {
        "entry_id": result.get("entry_id"),
        "title": result.get("title"),
        "summary_ai": result.get("summary_ai"),  # ✅ 添加
        "content": result.get("content"),
        "similarity": result.get("similarity"),
        "final_score": result.get("final_score"),  # ✅ 添加（混合搜索后）
        "scene_tags": result.get("scene_tags"),
    }
    for result in rag_results
]
```

---

## 执行顺序建议

### 第1天（6小时）
1. 数据库升舱（0.5h）
2. VectorClient.search_entries修改（1h）
3. 向量化逻辑修正（2h）
4. EntryService.update_entry修改（0.5h）
5. Memory0Service添加summary_ai支持（2h）

### 第2天（4小时）
6. 实现HybridSearchService（4h）

### 第3天（3小时）
7. 测试与验证（3小时）

---

## 验证方法

### 验证summary_ai字段

```bash
# 检查数据库结构
sudo -u postgres psql -d rag_db -c "\d entries"

# 查询测试数据
sudo -u postgres psql -d rag_db -c "SELECT entry_id, title, summary_ai FROM entries LIMIT 5;"
```

### 验证向量化逻辑

```bash
# 运行向量化脚本
cd /root/ai-factory
python3 refresh_titles_summaries_and_embeddings.py

# 检查向量
sudo -u postgres psql -d rag_db -c "SELECT entry_id, array_length(embedding, 1) FROM entry_embeddings LIMIT 5;"
```

### 验证混合搜索性能

```python
# 测试脚本
from ai_factory.agents.memory.hybrid_search_service import HybridSearchService
from ai_factory.agents.memory.entry_service import EntryService
from ai_factory.agents.memory.vector_client import VectorClient

entry_service = EntryService()
vector_client = VectorClient()
hybrid_search = HybridSearchService(entry_service, vector_client)

# 测试混合搜索
import time
start = time.time()
results = hybrid_search.search(
    query_text="如何配置GLM",
    query_embedding=[0.1, 0.2, ...],  # 实际向量
    metadata_filters={"project_code": "project_A"},
    top_k=10
)
end = time.time()

print(f"搜索耗时: {(end-start)*1000:.2f}ms")
print(f"返回结果: {len(results)}")
```

---

## 注意事项

1. **不要直接修改生产环境** - 先在测试环境验证
2. **备份数据库** - 执行ALTER TABLE前备份
3. **逐步迁移向量** - 避免一次性重生成所有向量
4. **充分测试** - 每个修改都要测试
5. **监控性能** - 修改后持续监控系统性能

---

## 需要帮助时

如遇到问题，请查阅：
- 详细审核报告：`/root/代码审核报告_记忆系统实现vs增强版设计.md`
- 增强版设计文档：`/root/ai-factory/Agent记忆系统详细设计与施工文档_增强版.md`
- 文档融合报告：`/root/文档融合完成报告.md`
