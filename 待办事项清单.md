# Agent 记忆系统待办事项清单

**文档版本:** v1.0  
**创建时间:** 2026-01-17  
**最后更新:** 2026-01-17

---

## 已完成事项（58 项）

### 核心服务实现（1-5）

- [x] EntryService 完整实现（create_entry, get_entry, search_similar）
- [x] SessionService 实现（append_message, get_recent_messages, create_session, get_session_info 等）
- [x] SectionService 实现（summarize_section + LLM 调用）
- [x] Memory0Service 实现（upsert_memory + process_entry + NEW/UPDATE/OVERRIDE/DUPLICATE 判定）
- [x] MemoryService 实现（get_context_for_turn, remember_explicitly）

### 数据库和基础设施（6-7）

- [x] 数据库表结构定义（chat_sessions, chat_messages, chat_sections, entries）
- [x] 向量客户端抽象（vector_client）

### 模块导出和测试（8-9）

- [x] 创建模块导出文件（__init__.py）和端到端测试脚本
- [x] 在设计文档中添加"当前实现状态 vs 目标设计"章节

### LLM 和 Embedding 集成（10-14）

- [x] 集成实际 LLM 服务（替换 SectionService 中的占位实现）
- [x] 集成实际 Embedding 服务（替换所有 _generate_embedding() 占位实现）
- [x] 更新模块导出（__init__.py 添加 LLMClient 导出）
- [x] 修复 DashScope Embedding 协议问题（OpenAI 兼容格式）
- [x] 优化 create_memory_stack() 共享 VectorClient 实例

### 任务队列和 Worker（15-22）

- [x] 创建 memory_tasks 表结构（任务队列存储）
- [x] 实现任务队列模块（task_queue.py）
- [x] 实现后台 Worker（memory_worker.py）
- [x] SectionService 集成任务队列（支持异步 Memory0）
- [x] 更新 create_memory_stack() 支持异步配置
- [x] 修复 create_memory_tasks_table.sql 索引语法（PostgreSQL 兼容）
- [x] 修复 task_queue.py connection_scope 使用方式
- [x] 修复 task_queue.py INTERVAL 参数写法（使用 make_interval）

### P0 和 P1 阶段（23）

- [x] 更新设计文档：标记 P0 和 P1 为"已完成并联调通过"

### P2 阶段：QACacheService 和配置集中化（24-30）

- [x] 实现 QACacheService（Q&A 缓存服务）
- [x] 配置集中化（Memory0 配置模块）
- [x] 统一 Section trigger_type 取值（修复语义不一致）
- [x] 编写端到端集成测试（验证完整流水线）
- [x] 修复 QACacheService quality_score 解析问题（支持 dict 和 float）
- [x] 修复 QACacheService query_qa() 返回类型（从 Dict 改为 QAInfo）
- [x] 创建 memory_tasks 表（手动执行 SQL）

### 代码路径整合和文档更新（31-35）

- [x] 更新设计文档：标记 P2 和 P3 为"已完成并联调通过"
- [x] 代码路径整合（删除旧路径 ai-factory/agents/memory/）
- [x] 更新测试文件导入路径（使用 ai_factory.ai_factory.agents.memory）
- [x] 更新设计文档：添加代码路径说明和 DashScope 配置说明
- [x] 提交代码路径整合和设计文档更新到 Git

### P3 阶段：Section 触发策略细化（36-45）

- [x] 实现 P3：Section 触发策略细化（check_and_trigger_section 方法）
- [x] 更新设计文档：标记 P3 为"已完成并联调通过"
- [x] 修复 P3：Section 触发策略的幂等性/防抖问题
- [x] 修复 __all__ 未导出 LLMClient 的问题
- [x] 编写 P3 集成测试（验证 Section 触发策略）
- [x] 修复 test_memory_pipeline.py sys.path 设置问题
- [x] 修复 P3：check_and_trigger_section 消息数量触发逻辑（首次场景）
- [x] 修复 P3：check_and_trigger_section 时区问题（offset-naive vs offset-aware）
- [x] 运行 P3 集成测试验证修复（全部通过）
- [x] 提交 P3 修复代码到 Git（commit e00953d）

### P4 阶段：Memory0 判定策略细化（46-52）

- [x] 实现 P4：Memory0 判定策略细化（LLM-based 关系判定）
- [x] 在 LLMClient 中添加 determine_memory_relation 方法
- [x] 在 Memory0Service 中集成 LLM-based 判定
- [x] 更新 create_memory_stack() 支持 enable_llm_judgment 参数
- [x] 运行测试验证 P4 实现（全部通过）
- [x] 提交 P4 代码到 Git（commit 77a1ab7）
- [x] 更新设计文档：标记 P4 为"已完成并联调通过"

### P5 阶段：多 Agent 语义切分（59-60）

- [x] 实现 P5：多 Agent 语义切分（Agent 识别与路由）
- [x] 创建 SectionAgent 类，包含 Agent1、Agent2、Agent3
- [x] 实现 Agent1：局部 Section 识别（analyze_local_context）
- [x] 实现 Agent2：宏观复核 + 话题标签（review_and_tag_sections）
- [x] 实现 Agent3：多视角关联重构（refactor_associations - 预留接口）
- [x] 实现协同工作流（analyze_session）
- [x] 编写 7 个测试用例，全部通过
- [x] 修复测试文件导入路径和配置参数
- [x] 提交 P5 代码到 Git（commit d22a30d）
- [x] 提交 P5 测试修复到 Git（commit d0c4201）
- [x] 更新设计文档：标记 P5 为"已完成并联调通过"
- [x] 更新待办事项清单：标记 P5 为"已完成"

### P3 修复：冷却时间窗和异步 Section 整理（53-57）

- [x] 修复 P3 问题 1：消息计数触发的幂等性缺失（添加冷却时间窗）
- [x] 修复 P3 问题 2：触发逻辑改为异步任务（入队而非同步整理）
- [x] 编写 P3 修复验证测试（冷却时间窗 + 异步 Section 整理）
- [x] 提交 P3 修复代码到 Git（commit 1cb8a3f）
- [x] 更新设计文档：标记 P3 修复为"已完成"（commit 840cf2b）

---

## 待办事项（0 项）

---

## Git 提交记录

| Commit Hash | 描述 | 时间 |
|------------|------|------|
| e00953d | P3 修复：Section 触发策略的幂等性/防抖问题 | 2026-01-16 |
| 77a1ab7 | P4 实现：Memory0 判定策略细化（LLM-based 关系判定） | 2026-01-16 |
| 1cb8a3f | P3 修复：添加冷却时间窗和异步 Section 整理 | 2026-01-17 |
| 840cf2b | 更新设计文档：标记 P3 修复为已完成 | 2026-01-17 |
| d22a30d | P5 实现：多 Agent 语义切分（SectionAgent） | 2026-01-17 |
| d0c4201 | P5 修复：SectionAgent 测试文件导入路径和配置参数 | 2026-01-17 |

---

## 阶段总结

### P0：LLM/Embedding 集成 ✅ 已完成
- 已实现 LLMClient 类，集成 DashScope Embedding (qwen3-embedding:4b) 和 DeepSeek LLM (deepseek-chat)
- 已修复 DashScope Embedding 协议问题，使用 OpenAI 兼容格式
- 已优化 create_memory_stack() 共享 VectorClient 实例

### P1：任务队列 & Worker ✅ 已完成
- 已创建 memory_tasks 表结构（PostgreSQL 兼容索引语法）
- 已实现 TaskQueue 抽象和 PostgresTaskQueue 实现
- 已实现 Memory0Worker 类，支持多线程、心跳、清理、重试机制
- 已修复 connection_scope 使用方式和 INTERVAL 参数写法
- 已集成到 SectionService，支持异步 Memory0 处理

### P2：QACacheService & 配置集中化 ✅ 已完成
- 已实现 QACacheService，支持 Q&A 缓存、查询、命中记录、统计、清理
- 已实现 ConfigManager，支持 Agent 配置注册/获取/更新/移除
- 已实现 Memory0Config 和 AgentMemoryConfig 数据类
- 已预定义 MEMORY0_PROFILES（CONSERVATIVE/AGGRESSIVE/OBSERVANT）
- 已修复 quality_score 解析问题和 query_qa() 返回类型问题

### P3：Section 触发策略细化 ✅ 已完成
- 已实现 check_and_trigger_section() 方法，支持三种触发策略
- 已添加 SectionTrigger 枚举（AUTO/MANUAL/TIMEOUT）
- 已更新 SectionService 默认 trigger_type 为 "auto"
- 已验证枚举值与 SQL 定义一致
- 已修复消息数量触发逻辑（首次场景）
- 已修复时区问题（offset-naive vs offset-aware）
- 已编写 4 个集成测试用例，验证触发策略
- **P3 修复：冷却时间窗和异步 Section 整理** ✅ 已完成
  - 已添加 last_section_triggered_at 字段到 chat_sessions 表
  - 已添加冷却时间窗检查（section_trigger_cooldown 参数，默认 300 秒）
  - 已添加异步 Section 整理支持（enable_async_section_summarize 参数）
  - 已修改 check_and_trigger_section() 方法支持冷却和异步模式
  - 已添加 SECTION_SUMMARIZE 任务类型到 TaskQueue
  - 已在 Memory0Worker 中添加 _process_section_task() 方法
  - 已更新 create_worker() 和 create_memory_stack() 函数
  - 已添加 create_worker_with_section() 便捷函数
  - 已更新测试文件，添加冷却时间窗和异步 Section 整理测试用例

### P4：Memory0 判定策略细化 ✅ 已完成
- 已在 LLMClient 中添加 determine_memory_relation() 方法
- 已在 Memory0Service 中集成 LLM-based 判定
- 已添加 enable_llm_judgment 参数（默认 False，保持向后兼容）
- 已实现 _handle_with_llm_judgment() 方法，支持 NEW/UPDATE/OVERRIDE/DUPLICATE 判定
- 已实现优雅降级：LLM 判定失败时回退到规则判定
- 已更新 create_memory_stack() 支持 enable_llm_judgment 参数
- 已运行测试验证 P4 实现（全部通过）

### P5：多 Agent 语义切分（Agent 识别与路由） ✅ 已完成
- Agent1：局部 Section 识别 Agent
- Agent2：宏观复核 + 话题标签 Agent
- Agent3（可选）：多视角关联重构 Agent
- ✅ 已实现 SectionAgent 类，包含 Agent1、Agent2、Agent3
- ✅ 已编写 7 个测试用例，全部通过
- ✅ 已集成 LLM 服务，支持显式结束语检测、LLM 分析、规则分析
- ✅ 已实现协同工作流（analyze_session）
- ✅ LLM 复核失败时优雅降级到规则复核

---

## 代码路径说明

**权威路径：** `ai-factory/ai_factory/agents/memory/`

**备份路径：** `ai-factory/agents/memory/`（已废弃）

**测试文件：** `ai-factory/ai_factory/agents/memory/test_memory_pipeline.py`

**导入语句：** `from ai_factory.ai_factory.agents.memory import ...`

---

## DashScope Embedding 配置

- **API 模式：** 原生 API（非兼容模式）
- **Base URL：** `https://dashscope.aliyuncs.com/api/v1`
- **API 路径：** `/services/embeddings/text-embedding/text-embedding`
- **模型名称：** `text-embedding-v3`
- **请求格式：** `{"model": "...", "input": {"texts": [text]}, "parameters": {"text_type": "document"}}`
- **响应解析：** `result["output"]["embeddings"][0]["embedding"]`

---

## 环境变量

- `EMBEDDING_MODEL_NAME=text-embedding-v3`（已在 .env 中配置）
- `DASHSCOPE_API_KEY`（已在 .env 中配置）

---

## 文档结束
