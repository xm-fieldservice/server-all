下面是一份可以作为**《项目总体说明》**的新文档草稿，你可以直接新建一个 `AI工厂-项目总体说明25-12-8.md`，或贴到现有文档里作为“第三份核心文档”。

---

# AI 工厂与广义知识库项目总体说明（草案）

> 配套文档：
> - 《表结构设计25-12-8.md》：负责数据库/事实层/解释层/业务层建模  
> - 《项目工程结构25-12-8.md》：负责代码工程与模块划分  
> - **《本文件》：负责“整个项目想做什么、整体架构与核心理念”**

---

## 1. 项目愿景与核心理念

### 1.1 最高目标：构建一个“广义知识库”

本项目的**最高层核心**不是 AI 工厂本身，而是一个**广义知识库**：

- 内容范围不限于 AI 或技术，而是：
  - 生活知识、工作经验；
  - 生产/运营知识；
  - 规则与规律（政策、制度、业务规则、流程）；
  - 人员信息、组织架构、角色职责；
  - 财务信息、平衡计分卡（Balanced Scorecard）相关指标；
  - 以及未来任何类型的知识与事实。

- 我们把“知识”视为一切记录和结构化信息的**最高抽象层**：  
  不论是一条工作流水、一份财务报表、一个 Agent 配置、一条经验总结，最终都要沉淀为“知识库中的一格”。

### 1.2 AI 工厂的角色：为知识库服务的“工具集合”

- **AI 工厂不是最终目的，而是“工具工厂”**：
  - 负责生产、组装和维护各种 AI 组件：
    - 模型配置、本地模型/API 模型调用封装；
    - Agent / Team 组合；
    - 工具（Tools）、MCP、向量库（Memory / RAG）；
    - 分析与评估脚本等。
  - 这些组件的存在目的只有一个：  
    **更好地“管理、演进和利用”广义知识库。**

- 总结关系：
  - **广义知识库 = 最高层目标**  
  - **AI 工厂 = 为知识库工作的一整套工具与流水线**

---

## 2. 组装 vs 货架：新项目的重点转移

### 2.1 底层组装已交给 AI 工厂与规范

- 通过 AutoGen 0.7.1 / ai-factory 等规范：
  - Agent/Team/工具/向量库的组合可以由底层逻辑 + AI 助手自动按规范组装；
  - 人工不再需要每次手工拼接 JSON 或硬编码链路。

- 因此，**“会不会手工组装 Agent/工具”已经不是主要矛盾**。

### 2.2 新项目的重点：货架式管理 + 性能对比

新项目更强调的是：

1. **货架式管理（Shelf Management）**
   - 把所有“可用能力”和“沉淀知识”都放到一个**广义货架**上：
     - 模型、Agent、Team；
     - 工具、MCP、向量库；
     - 评测方案、评测结果；
     - 各种业务知识、规则、经验。
   - 通过**分类 / 标签 / 结构化视图**来管理它们：
     - 按任务类型、领域、数据来源、成熟度等维度分类；
     - 支持检索、过滤、版本管理。

2. **性能与功能管理（对比与评估）**
   - 对同一类任务/场景：
     - 对比不同模型、不同 Agent 组合、不同工具/向量库方案的**性能表现**；
     - 不只是主观感觉，而是有指标（准确率、召回率、时延、成本等）。
   - 把这些评测数据也写入知识库，变成**“有关能力的知识”**：
     - 哪个方案在什么条件下表现更好；
     - 哪些方案只是实验性，哪些可以进入生产。

---

## 3. 广义货架与数据库的关系

### 3.1 原始数数据库 = 货架的物理底座

- 已在《表结构设计》中规划：
  - **事实层（entries）**：  
    - 所有真实发生的记录（笔记、任务、议题、工作流事件、评测流水等）；
    - 用 `space_type + parent_entry_id` 表达树形结构与“空间标记”；
  - **解释层（map_snapshots）**：  
    - 对事实层的结构化理解（战略地图、项目脑图、难点拆解、能力地图）；
  - **业务事实层（biz.* schema）**：  
    - 请假、报销、借款、工资等可核算业务事实。

- **本项目把这一套数据库视作“广义货架的物理层”**：
  - 货架上的所有“货物”（组件、知识、性能记录）最终都以某种形式落到这些表里；
  - 上层的“货架视图”只是对这些底层记录的聚合与重组。

### 3.2 货架视图 = 逻辑层的分类与抽象

- 在逻辑层上，我们有多个“货架视图”：
  - **组件货架**：
    - 按类别展示模型、Agent、Team、工具、向量库、评测脚本等；
  - **性能货架**：
    - 按任务/场景展示各类组合的性能对比；
  - **知识货架**：
    - 把与生活、生产、规则、人员、财务等相关的知识统一呈现。

- 这些视图本质上是：
  - 对 `entries` / `map_snapshots` / `biz.*` 等表的**投影和聚合**；
  - 通过标签、分类、过滤规则形成不同角度的“货架”。

---

## 4. 与问答系统的关系

### 4.1 问答系统是广义知识库的“前台入口”

- 问答系统并不是一个单独的项目，而是：
  - **广义知识库的主入口和交互层**；
  - 一个“面向人”的统一接口：
    - 用户可以用自然语言提问；
    - 系统基于知识库和底层组件给出答案。

### 4.2 问答系统的定位

- 作为**“项链库”或“门面”**：
  - 将广义知识库中的事实层、解释层、业务层内容**串起来**；
  - 把后端的各种组件（RAG、Agent、业务表）统一隐藏在一个对用户友好的问答界面后面。

- 对 AI 工厂而言：
  - 问答系统用到的各种 Agent、检索流程、工具、业务解析流程，  
    都应该来自 AI 工厂维护的“组件货架”和“评测结果”，而不是临时拼凑。

---

## 5. 项目的四大核心文档与架构设计起点

### 5.1 四份核心文档的角色

1. **《表结构设计25-12-8》**  
   - 定义事实层 / 解释层 / 业务层 / 评测流水的表结构与字段语义；
   - 是**“货架物理层 + 数据语义层”**的说明书。

2. **《项目工程结构25-12-8》**  
   - 定义 `ai-factory` 核心工程、各应用工程（desktop_app、三栏工具等）的代码结构；
   - 划清：
     - AI 工厂（能力与编排层）  
     - 应用工程（UI / 具体业务场景）。

3. **《AI工厂-对外接口与集成规范》**  
   - 说明 AI 工厂对外提供的接口与能力，包括：
     - `entries` 事实层访问与 Node Query；  
     - RAG / WEB 问答能力；
   - 是**所有外部系统与 AI 工厂对接时唯一的接口总规范**。

4. **《项目总体说明》（本文件）**  
   - 说明整个项目**想解决什么问题、核心理念是什么**；
   - 明确：
     - **最高目标是广义知识库**；  
     - AI 工厂是为这个目标服务的工具工厂；  
     - 新项目重点在“货架式管理 + 性能对比”。

### 5.2 从四份文档回到“项目架构设计”

> 说明：除以上四份核心文档外，其它关于 AI 工厂的 v0 / 草案类文档（例如《AI工厂架构清单和职责草案》、docs/架构清单_AI工厂_v0、docs/三栏_AI工厂_协议清单_v0、docs/entries_ingest_对接说明_v0 等）均视为**历史或补充材料**，不再作为权威规范，遇到冲突时以本节所列四份核心文档为准。

在这四份文档清晰之后，我们可以回到最初的问题：**能否据此设计一套项目架构？**

大致分层会是：

- **顶层：广义知识库 & 问答系统**
  - 面向用户的统一问答入口；
  - 由知识货架、性能货架、组件货架共同支撑。

- **中层：AI 工厂能力层**
  - `ai_factory.agents` / `ai_factory.rag` / `ai_factory.db` / `ai_factory.testing` 等模块；
  - 负责组件生产、组装、评测、导出。

- **底层：数据库与基础设施**
  - PostgreSQL + pgvector 等；
  - `entries` / `map_snapshots` / `biz.*` 表及其迁移、备份、监控。

---

## 6. 小结：一句话版本

- **最高层**：  
  一个包罗万象的广义知识库，涵盖生活、生产、规则、人员、财务等所有类型的知识。

- **中间层**：  
  一个“AI 工厂”，负责生产和管理各种 Agent/模型/工具/向量库等组件，  
  并通过**货架式管理 + 性能对比**，帮助我们挑选最合适的方案来服务知识库。

- **底层**：  
  以原始数数据库为物理货架，把所有事实、解释、业务数据和评测结果统一存储为可追溯的记录。

---

如果你确认这个“项目总体说明”的方向基本准确，下一步我们可以基于这三份文档：

- 一起画出一个**项目架构图/模块图**（文字版也可以），  
- 或先从“货架 + 性能对比”这一块，设计一两个具体的表/模块（例如：评测流水表、组件货架表）作为第一批落地实现。