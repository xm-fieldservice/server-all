# 企业微信部分开发计划 v1.2

**文档版本**: v1.2
**制定日期**: 2026-01-25
**制定人**: 项目组
**目标版本**: v1.2（与Agent记忆系统集成）

---

## 执行摘要

### 项目目标

将企业微信系统与AI工厂的Agent记忆系统集成，构建统一的数据底座，实现：
1. **统一数据存储** - 替代当前内存存储方案，实现持久化
2. **智能检索能力** - 基于向量+关键字的混合搜索
3. **知识自动沉淀** - 从聊天记录自动提炼高质量知识
4. **企业级安全** - 四层数据隔离+RLS保障

### 开发周期

| 阶段 | 时间 | 人力 | 里程碑 |
|------|------|------|--------|
| **阶段一：MVP集成** | 1-2周 | 1-2人 | 消息可入库、基础检索可用 |
| **阶段二：完整功能** | 2-3周 | 2人 | 自动整理、混合搜索、问答增强 |
| **阶段三：高级功能** | 4周 | 2-3人 | 知识自动提炼、数据分析面板 |

### 关键指标

- **可行性评分**: ⭐⭐⭐⭐⭐ 4.8/5（强烈推荐）
- **技术风险**: 极低（仅需添加客户端调用）
- **投入产出比**: 极高（约2周投入换来长期价值）

---

## 一、项目背景

### 1.1 当前状况

**已完成功能**：
- ✅ 企业微信回调通道开发与测试
- ✅ 组织架构同步底座（[`wecom_org.py`](wechat-workspace/wecom_gateway/wecom_org.py)）
- ✅ 基础群聊系统（[`group_chat.py`](wechat-workspace/wecom_gateway/group_chat.py)，内存存储）
- ✅ 消息处理器框架（[`handlers/note_bot.py`](wechat-workspace/wecom_gateway/handlers/note_bot.py), [`handlers/qa_bot.py`](wechat-workspace/wecom_gateway/handlers/qa_bot.py)）

**待解决问题**：
- ⚠️ 数据存储在内存，服务重启丢失
- ⚠️ 缺乏向量检索能力
- ⚠️ 数据孤岛，无法与其他系统打通
- ⚠️ 缺乏知识自动沉淀机制

### 1.2 集成目标

| 目标 | 说明 | 优先级 |
|------|------|--------|
| **统一数据底座** | 所有通道数据汇聚到Agent记忆系统 | P0 |
| **持久化存储** | 替代内存存储，数据不丢失 | P0 |
| **智能检索** | 向量+关键字混合搜索 | P1 |
| **知识沉淀** | 自动从聊天记录提炼知识 | P1 |
| **多租户隔离** | 企业级安全保障 | P0 |
| **可扩展架构** | 为未来通道接入打好基础 | P1 |

---

## 二、架构设计

### 2.1 总体架构

```
┌──────────────────────────────────────────────────────────┐
│                    企业微信服务器                          │
└──────────────────────────────────────────────────────────┘
                            │ 回调
                            ↓
┌──────────────────────────────────────────────────────────┐
│              wechat-gateway (现有服务)                     │
│  ┌────────────────────────────────────────────┐          │
│  │  回调处理层                                 │          │
│  │  - 签名验证 (app.py)                        │          │
│  │  - 消息解密                                 │          │
│  │  - 路由分发 (registry.py)                  │          │
│  └────────────────────────────────────────────┘          │
│                      ↓                                    │
│  ┌────────────────────────────────────────────┐          │
│  │  消息处理器层                               │          │
│  │  - NoteBot Handler (handlers/note_bot.py)  │          │
│  │  - QABot Handler (handlers/qa_bot.py)      │          │
│  │  - Group Handler (group_chat.py)           │          │
│  └────────────────────────────────────────────┘          │
│                      ↓                                    │
│  ┌────────────────────────────────────────────┐          │
│  │  Memory Client (新增) ⭐                    │          │
│  │  - HTTP客户端                               │          │
│  │  - 重试逻辑                                 │          │
│  │  - Scene Tags构造                           │          │
│  └────────────────────────────────────────────┘          │
└──────────────────────────────────────────────────────────┘
                            │ HTTP
                            ↓
┌──────────────────────────────────────────────────────────┐
│               ai-factory (现有服务)                        │
│  ┌────────────────────────────────────────────┐          │
│  │  EntryService API (已有)                   │          │
│  │  - POST /entries/ingest                     │          │
│  │  - POST /qa/rag                             │          │
│  └────────────────────────────────────────────┘          │
│                      ↓                                    │
│  ┌────────────────────────────────────────────┐          │
│  │  Agent记忆系统核心                          │          │
│  │  - L1: chat_sessions + chat_messages        │          │
│  │  - L2: chat_sections (整理)                 │          │
│  │  - L3: entries + entry_embeddings          │          │
│  │  - L4: qa_query_index (缓存)                │          │
│  └────────────────────────────────────────────┘          │
└──────────────────────────────────────────────────────────┘
                            │
                            ↓
┌──────────────────────────────────────────────────────────┐
│            PostgreSQL + pgvector (数据库)                 │
│  - chat_sessions (L1)                                    │
│  - chat_messages (L1)                                    │
│  - chat_sections (L2)                                    │
│  - entries (L3)                                          │
│  - entry_embeddings (L3)                                 │
│  - qa_query_index (L4)                                   │
└──────────────────────────────────────────────────────────┘
```

### 2.2 数据映射

| 企业微信数据类型 | Agent记忆层级 | 存储位置 | 说明 |
|-----------------|--------------|---------|------|
| **实时聊天消息** | L1: 短期记忆层 | `chat_sessions` + `chat_messages` | 原始对话、碎片化内容 |
| **工作笔记** | L2 → L3 | Section处理 → `entries` | 用户发送的"记：..."消息 |
| **问答对话** | L1 + L3 | `chat_messages` + `entries` | 问题和答案都持久化 |
| **整理后的会话** | L3: 大库存储 | `entries` + `entry_embeddings` | 高质量知识条目，可检索 |
| **FAQ答案** | L4: Q&A缓存 | `qa_query_index` | 快速响应重复问题 |

### 2.3 四层数据隔离

```python
# 企业微信场景示例
isolation_context = {
    # L1: 用户隔离
    "user_id": "wecom:zhangsan",

    # L2: Agent类型隔离
    "agent_type": "wecom_note_bot",  # 或 "wecom_qa_bot"

    # L3: Agent实例隔离
    "agent_instance_id": "dept_001_note",  # 技术部的笔记助手实例

    # L4: 会话隔离
    "session_id": "wecom_session_2026012301"
}
```

---

## 三、Scene Tags规范

### 3.1 完整结构

```python
scene_tags = {
    # ========== 通道来源 ==========
    "channel": ["wecom_note_bot"],           # 或 wecom_qa_bot, wecom_dm, wecom_group
    "source_system": "wecom",
    "source_app": "wechat_gateway",

    # ========== 语义类型 ==========
    "semantic_type": ["note"],               # 或 qa_question, qa_answer, human_chat

    # ========== 对话关系 ==========
    "peer": ["wecom:user_zhangsan"],         # 对话对象
    "conversation": ["conv_wecom_xxx"],      # 会话标识

    # ========== 组织架构 ==========
    "department": [1, 2],                    # 部门ID列表
    "department_name": ["技术部", "研发组"],  # 部门名称
    "department_path": ["公司/技术部/研发组"], # 完整路径

    # ========== 项目关联 ==========
    "project": ["project_X"],                # 项目代码
    "project_hint": "接口联调",              # 项目提示

    # ========== 原始元数据 ==========
    "origin_id": "wecom_msg_12345",          # 企业微信原始消息ID
    "message_type": "text",                  # 消息类型

    # ========== 业务标签 ==========
    "tags": ["urgent", "todo"],              # 自定义标签
    "importance_score": 0.8                  # 重要性评分
}
```

### 3.2 场景示例

#### 场景1: 工作笔记助手（Note Bot）

```python
{
    "channel": ["wecom_note_bot"],
    "semantic_type": ["note"],
    "peer": ["wecom:note_bot"],
    "conversation": ["conv_wecom_note_20260123_001"],
    "department": [1],
    "department_name": ["技术部"],
    "project": ["project_X"],
    "origin_id": "wecom_note_msg_12345"
}
```

#### 场景2: AI问答助手（QA Bot）

```python
# 用户问题
{
    "channel": ["wecom_qa_bot"],
    "semantic_type": ["qa_question"],
    "peer": ["wecom:qa_bot"],
    "conversation": ["conv_wecom_qa_20260123_002"],
    "department": [1],
    "origin_id": "wecom_qa_msg_12346"
}

# AI回答
{
    "channel": ["wecom_qa_bot"],
    "semantic_type": ["qa_answer"],
    "peer": ["wecom:qa_bot"],
    "conversation": ["conv_wecom_qa_20260123_002"],
    "department": [1],
    "referenced_entries": ["entry_001", "entry_002"]
}
```

---

## 四、开发任务分解

### 阶段一：最小可用集成（MVP）

**目标**: 打通企业微信 → Agent记忆系统的基本链路
**时间**: 1-2周
**人力**: 1-2人

#### 任务1.1：创建Memory Client模块 (2-3天)

**文件**: `wecom_gateway/memory_client.py`

**功能**:
- HTTP客户端封装（httpx）
- 重试逻辑（tenacity）
- Scene Tags构造辅助函数
- 日志记录

**API接口**:
```python
class MemoryClient:
    async def create_session(...)
    async def add_message(...)
    async def trigger_section_summary(...)
    async def search_entries(...)
    async def close()
```

**验收标准**:
- [ ] 能成功调用ai-factory的API
- [ ] 重试机制正常工作
- [ ] 日志记录完整

#### 任务1.2：改造Note Bot处理器 (2-3天)

**文件**: `wecom_gateway/handlers/note_bot.py`

**改造内容**:
- 集成Memory Client
- 获取用户部门信息
- 构造正确的scene_tags
- 写入L1短期记忆
- 触发Section整理（可选）

**验收标准**:
- [ ] 笔记消息正确写入Agent记忆系统
- [ ] scene_tags包含正确的部门信息
- [ ] 数据隔离正常工作

#### 任务1.3：改造QA Bot处理器 (2-3天)

**文件**: `wecom_gateway/handlers/qa_bot.py`

**改造内容**:
- 集成Memory Client
- 调用混合搜索API
- 构造问答上下文
- 问题+答案都入库
- 通过企业微信回复用户

**验收标准**:
- [ ] 问答功能正常工作
- [ ] 搜索结果准确
- [ ] 问答对话正确入库

#### 任务1.4：配置与部署 (1天)

**环境变量配置**:
```bash
# .env
AI_FACTORY_BASE_URL=http://localhost:8001
WECOM_CORP_ID=xxx
WECOM_APP_SECRET=xxx
WECOM_NOTE_AGENT_ID=xxx
WECOM_QA_AGENT_ID=xxx
```

**验收标准**:
- [ ] 环境变量配置正确
- [ ] 服务能正常启动
- [ ] 数据库连接正常

#### 任务1.5：基础测试 (2天)

**测试用例**:
1. 消息写入测试
   - 发送笔记消息，验证入库
   - 发送问答消息，验证入库和回复

2. 数据隔离测试
   - 不同用户数据隔离
   - 不同部门数据隔离

3. 基础检索测试
   - 关键字搜索
   - 向量搜索

**验收标准**:
- [ ] 所有测试用例通过
- [ ] 无严重bug

---

### 阶段二：完整功能集成

**时间**: 2-3周
**人力**: 2人

#### 任务2.1：Section自动整理 (3-5天)

**功能**:
- 配置自动触发规则（N条消息或M分钟）
- 实现整理策略
- 测试整理效果

**验收标准**:
- [ ] 自动整理功能正常
- [ ] 整理质量符合预期
- [ ] 整理后的内容可检索

#### 任务2.2：混合搜索集成 (3-4天)

**功能**:
- 按部门检索
- 按时间范围检索
- 按项目检索
- 组合过滤

**验收标准**:
- [ ] 搜索功能正常
- [ ] 搜索结果准确
- [ ] 性能满足要求

#### 任务2.3：问答机器人增强 (3-5天)

**功能**:
- 历史记录检索
- QA缓存利用
- 答案质量优化

**验收标准**:
- [ ] 问答质量提升
- [ ] 响应速度提升
- [ ] 用户满意度提升

#### 任务2.4：Web管理界面 (5-7天)

**功能**:
- 消息历史查看
- 知识条目管理
- 统计分析面板

**验收标准**:
- [ ] 界面功能完整
- [ ] 用户体验良好
- [ ] 性能满足要求

---

### 阶段三：高级功能

**时间**: 4周
**人力**: 2-3人

#### 任务3.1：知识自动提炼 (1周)

**功能**:
- FAQ自动提取
- 重复问题识别
- 标准答案生成

#### 任务3.2：跨部门知识共享 (1周)

**功能**:
- 基于权限的知识可见性
- 部门间知识推荐
- 知识审核机制

#### 任务3.3：数据分析与洞察 (1-2周)

**功能**:
- 工作活跃度统计
- 知识沉淀质量评估
- 热点话题分析
- 用户行为分析

---

## 五、技术实施细节

### 5.1 Memory Client实现

```python
# wecom_gateway/memory_client.py

import httpx
import logging
from typing import Dict, Any, List, Optional
from tenacity import retry, stop_after_attempt, wait_exponential

logger = logging.getLogger(__name__)


class MemoryClient:
    """Agent记忆系统HTTP客户端"""

    def __init__(self, base_url: str, timeout: int = 30):
        self.base_url = base_url.rstrip('/')
        self.timeout = timeout
        self.client = httpx.AsyncClient(timeout=timeout)

    @retry(
        stop=stop_after_attempt(3),
        wait=wait_exponential(multiplier=1, min=2, max=10)
    )
    async def create_session(
        self,
        user_id: str,
        agent_type: str,
        agent_instance_id: str,
        metadata: Optional[Dict[str, Any]] = None
    ) -> str:
        """创建会话（L1）

        Returns:
            session_id
        """
        payload = {
            "user_id": user_id,
            "agent_type": agent_type,
            "agent_instance_id": agent_instance_id,
            "metadata": metadata or {}
        }

        response = await self.client.post(
            f"{self.base_url}/memory/sessions",
            json=payload
        )
        response.raise_for_status()
        result = response.json()

        logger.info(f"Created session: {result['session_id']}")
        return result['session_id']

    @retry(
        stop=stop_after_attempt(3),
        wait=wait_exponential(multiplier=1, min=2, max=10)
    )
    async def add_message(
        self,
        session_id: str,
        role: str,
        content: str,
        scene_tags: Dict[str, Any],
        extra_meta: Optional[Dict[str, Any]] = None
    ) -> str:
        """添加消息到短期记忆（L1）

        Args:
            session_id: 会话ID
            role: user/assistant/system
            content: 消息内容
            scene_tags: 场景标签（Level 4）
            extra_meta: 额外元数据

        Returns:
            message_id
        """
        payload = {
            "session_id": session_id,
            "role": role,
            "content": content,
            "scene_tags": scene_tags,
            "extra_meta": extra_meta or {}
        }

        response = await self.client.post(
            f"{self.base_url}/memory/messages",
            json=payload
        )
        response.raise_for_status()
        result = response.json()

        logger.info(f"Added message: {result['message_id']}")
        return result['message_id']

    @retry(
        stop=stop_after_attempt(3),
        wait=wait_exponential(multiplier=1, min=2, max=10)
    )
    async def trigger_section_summary(
        self,
        session_id: str,
        trigger_type: str = "manual"
    ) -> Dict[str, Any]:
        """触发会话整理（L2）

        Args:
            session_id: 会话ID
            trigger_type: auto/manual/timeout

        Returns:
            section_info with summary_entry_id
        """
        payload = {
            "session_id": session_id,
            "trigger_type": trigger_type
        }

        response = await self.client.post(
            f"{self.base_url}/memory/sections/summarize",
            json=payload
        )
        response.raise_for_status()
        result = response.json()

        logger.info(f"Section summarized: {result['section_id']}")
        return result

    @retry(
        stop=stop_after_attempt(3),
        wait=wait_exponential(multiplier=1, min=2, max=10)
    )
    async def search_entries(
        self,
        query: str,
        user_id: str,
        agent_type: str,
        filters: Optional[Dict[str, Any]] = None,
        top_k: int = 10
    ) -> List[Dict[str, Any]]:
        """混合搜索（L3）

        Args:
            query: 查询文本
            user_id: 用户ID（数据隔离）
            agent_type: Agent类型（数据隔离）
            filters: scene_tags过滤条件
            top_k: 返回结果数量

        Returns:
            List of entries
        """
        payload = {
            "query": query,
            "user_id": user_id,
            "agent_type": agent_type,
            "filters": filters or {},
            "top_k": top_k
        }

        response = await self.client.post(
            f"{self.base_url}/memory/search",
            json=payload
        )
        response.raise_for_status()
        result = response.json()

        logger.info(f"Search returned {len(result['entries'])} entries")
        return result['entries']

    async def close(self):
        """关闭客户端"""
        await self.client.aclose()
```

### 5.2 消息处理器改造示例

```python
# wecom_gateway/handlers/note_bot.py

from memory_client import MemoryClient
from wecom_org import get_user_detail

memory_client = MemoryClient(
    base_url=os.getenv("AI_FACTORY_BASE_URL", "http://localhost:8001")
)


async def handle_note_message(message: Dict[str, Any]) -> str:
    """处理笔记消息（记：...）"""

    user_id = message.get("FromUserName")
    content = message.get("Content", "")

    # 获取用户信息（包括部门）
    user_info = get_user_detail(user_id)
    if not user_info:
        return "用户信息获取失败"

    # 解析项目代码（如果有）
    project_code = extract_project_code(content)

    # 构造scene_tags
    scene_tags = {
        "channel": ["wecom_note_bot"],
        "semantic_type": ["note"],
        "peer": ["wecom:note_bot"],
        "department": user_info.get("department", []),
        "department_name": [get_dept_name(d) for d in user_info.get("department", [])],
        "origin_id": message.get("MsgId"),
        "message_type": "text"
    }

    if project_code:
        scene_tags["project"] = [project_code]

    # 构造extra_meta
    extra_meta = {
        "user_name": user_info.get("name"),
        "position": user_info.get("position"),
        "send_time": message.get("CreateTime")
    }

    # 隔离上下文
    isolation_context = {
        "user_id": f"wecom:{user_id}",
        "agent_type": "wecom_note_bot",
        "agent_instance_id": f"dept_{user_info['department'][0]}_note"
    }

    # 获取或创建会话
    session_id = await get_or_create_session(
        user_id=isolation_context["user_id"],
        agent_type=isolation_context["agent_type"],
        agent_instance_id=isolation_context["agent_instance_id"]
    )

    # 添加消息到L1
    message_id = await memory_client.add_message(
        session_id=session_id,
        role="user",
        content=content,
        scene_tags=scene_tags,
        extra_meta=extra_meta
    )

    # 检查是否需要触发Section整理
    message_count = await get_session_message_count(session_id)
    if message_count >= 5:  # 可配置
        await memory_client.trigger_section_summary(
            session_id=session_id,
            trigger_type="auto"
        )

    return f"笔记已保存 (ID: {message_id[:8]}...)"
```

### 5.3 配置示例

```bash
# .env

# AI Factory配置
AI_FACTORY_BASE_URL=http://localhost:8001
AI_FACTORY_API_KEY=your_api_key_here

# 数据库配置（如果直接连接）
MEMORY_DB_HOST=localhost
MEMORY_DB_PORT=5433
MEMORY_DB_NAME=rag_db
MEMORY_DB_USER=rag_user
MEMORY_DB_PASSWORD=your_password

# Section整理配置
AUTO_SECTION_MESSAGE_THRESHOLD=5  # 多少条消息触发整理
AUTO_SECTION_TIME_THRESHOLD=300   # 多少秒无消息触发整理

# 缓存配置
MEMORY_CACHE_TTL=3600  # session缓存时间
```

---

## 六、权限管理与安全控制

### 6.1 双层保障机制

```
第一层：数据库层（RLS）
  └─ PostgreSQL Row Level Security
  └─ 强制隔离，无法绕过
  └─ 防止SQL注入等攻击

第二层：应用层（Scene Tags）
  └─ 显式过滤条件
  └─ 业务逻辑灵活控制
  └─ 支持复杂权限规则
```

### 6.2 数据可见性规则

| 用户角色 | 可见数据范围 | 实现机制 |
|----------|-------------|----------|
| **普通员工** | 本人 + 本部门 | `user_id` + `department` 过滤 |
| **部门负责人** | 本部门 + 子部门 | `is_department_leader()` + 递归查询 |
| **项目成员** | 项目相关（跨部门） | `project` 标签授权 |
| **知识管理员** | 公司级知识库 | `visibility: company` + 特殊角色 |
| **系统管理员** | 所有数据 | RLS bypass (角色级别) |

### 6.3 审计日志

**审计事件类型**:
- 搜索知识条目
- 创建/修改/删除知识条目
- 跨部门访问
- 权限拒绝
- 管理员操作

---

## 七、风险与挑战

### 7.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| **消息量大，L1存储压力** | 中 | 高 | 热数据保留策略+冷数据归档 |
| **向量计算性能** | 中 | 中 | 批量向量化+异步处理 |
| **数据隔离失效** | 高 | 低 | RLS双重保障+定期审计 |
| **权限绕过攻击** | 高 | 低 | 数据库层+应用层双重检查 |
| **API调用失败** | 中 | 中 | 重试机制+降级方案 |

### 7.2 性能挑战

**挑战1: 消息写入延迟**
- 解决方案: 异步队列处理

**挑战2: 向量搜索性能**
- 解决方案: pgvector优化 + 混合搜索

**挑战3: Section整理耗时**
- 解决方案: 异步执行，不阻塞消息写入

---

## 八、测试计划

### 8.1 单元测试

- [ ] Memory Client各方法测试
- [ ] Scene Tags构造测试
- [ ] 权限检查逻辑测试

### 8.2 集成测试

- [ ] 消息写入端到端测试
- [ ] 数据隔离测试
- [ ] 搜索功能测试
- [ ] 问答功能测试

### 8.3 性能测试

- [ ] 消息写入压力测试
- [ ] 搜索性能测试
- [ ] 并发测试

### 8.4 安全测试

- [ ] 权限绕过测试
- [ ] SQL注入测试
- [ ] 跨部门数据泄露测试

---

## 九、部署计划

### 9.1 部署环境

| 环境 | 用途 | 配置 |
|------|------|------|
| **开发环境** | 本地开发 | 单机部署 |
| **测试环境** | 功能测试 | 模拟生产配置 |
| **生产环境** | 正式运行 | 高可用配置 |

### 9.2 部署步骤

1. **代码部署**
   - 更新代码到服务器
   - 安装依赖
   - 配置环境变量

2. **数据库迁移**
   - 运行迁移脚本
   - 验证表结构

3. **服务启动**
   - 启动ai-factory服务
   - 启动wechat-gateway服务
   - 验证健康检查

4. **灰度发布**
   - 先开放给部分用户
   - 收集反馈
   - 逐步扩大范围

---

## 十、项目里程碑

| 里程碑 | 时间 | 交付物 | 负责人 |
|--------|------|--------|--------|
| **M1: MVP完成** | 第2周 | 消息可入库、基础检索可用 | 开发团队 |
| **M2: 完整功能** | 第5周 | 自动整理、混合搜索、问答增强 | 开发团队 |
| **M3: 高级功能** | 第9周 | 知识自动提炼、数据分析面板 | 开发团队 |
| **M4: 生产上线** | 第10周 | 灰度发布完成，系统稳定 | 运维团队 |

---

## 十一、附录

### 11.1 参考文档

1. [企业微信内部通讯与知识库系统 v1 需求规格说明书](wechat-workspace/《企业微信内部通讯与知识库系统 v1 需求规格说明书（草案）》.md)
2. [企业微信与Agent记忆系统集成可行性评估报告](wechat-workspace/企业微信与Agent记忆系统集成可行性评估报告.md)
3. [AI工厂-对外接口与集成规范-服务器版](ai-factory/《AI工厂-对外接口与集成规范-服务器版》.md)
4. [Agent记忆系统完整设计与实施文档_V3融合版](ai-factory/Agent记忆系统完整设计与实施文档_V3融合版.md)

### 11.2 关键文件

| 文件 | 说明 |
|------|------|
| `wecom_gateway/app.py` | 主应用，回调处理 |
| `wecom_gateway/wecom_org.py` | 组织架构同步 |
| `wecom_gateway/handlers/note_bot.py` | 笔记机器人处理器 |
| `wecom_gateway/handlers/qa_bot.py` | 问答机器人处理器 |
| `wecom_gateway/memory_client.py` | **新增**：记忆系统客户端 |

### 11.3 联系人

| 角色 | 姓名 | 联系方式 |
|------|------|---------|
| **技术负责人** | - | - |
| **产品负责人** | - | - |
| **ai-factory对接人** | - | - |

---

**文档状态**: ✅ 已完成
**评审状态**: ⏳ 待评审
**批准状态**: ⏳ 待批准
