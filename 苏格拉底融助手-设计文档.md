# 苏格拉底融助手（融助手）总体设计文档

> 版本：v1.0（设计对齐稿）  
> 范围：围绕「议题」的苏格拉底式多轮问答与结构化梳理  
> 目标：作为后续实现与评审的统一对齐文档

---

## 1. 角色定位与使用场景

### 1.1 角色定位

- 名称：**融助手（苏格拉底助手）**  
- 核心能力：
  - 采用苏格拉底式多轮提问，引导用户围绕某个「议题」澄清背景、目标、约束、方案、风险等信息；
  - 将多轮问答沉淀为一个 **结构化的议题/项目卡片** + **可阅读的说明文档**；
  - 支持在同一议题下**动态修改意图/视角**，并在新的提问空间内自然接续提问。

### 1.2 使用前置条件与约束

- **必须在标签包含 `议题` 的上下文中才能启动**：
  - 若当前选中的内容/会话未打上标签 `议题`，后端应拒绝启动融助手：
    - 返回提示，例如：
      > “苏格拉底融助手仅支持在『议题』场景下使用。请先为当前内容添加标签【议题】，然后再重新启动融助手。”
- 建议对所有由融助手生成的重要记录（阶段总结、最终说明等）：
  - 写入现有 `entries` 表及向量库；
  - 标签中至少包含：
    - `议题`
    - `融助手`（建议专用子标签，用于区分普通笔记）。

### 1.3 核心使用场景

- 典型场景：
  - 新项目/新议题的背景梳理；
  - 架构评审议题的要点整理；
  - 制度设计 / 规则讨论议题的结构化表达；
  - 复盘总结类议题（某个阶段、某个事件的系统性复盘）。

用户行为模式：

- **外层**：用户定义议题、描述目标、可以随时调整整体意图/结构，决定何时生成总结、何时结束。
- **内层**：融助手在既定“提问空间”内自动生成一轮轮苏格拉底式问题，用户只需回答与微调。

---

## 2. 总体架构分层

融助手的设计采用三层架构 + 多角色（逻辑多 Agent）：

### 2.1 三层架构

1. **会话层（Session Layer）**
   - 管理每个「议题会话」的：
     - `session_id`
     - 对话历史 `messages`
     - 结构化议题卡片 `project_profile`
     - 版本信息与元数据 `meta`

2. **上下文与记忆层（Context & Memory Layer）**
   - **短期上下文**：
     - 当前会话最近 N 轮消息（如 10–20 条），用于保持多轮对话连贯；
     - 更早的记录可以摘要成一条“早期对话总结”，附在 system / 背景说明中。
   - **中长期记忆**：
     - 复用现有 DB + 向量库（entries + embeddings）：
       - 通过 `project_code` + 标签 `议题` +（可选）`融助手` 做 RAG；
       - 将检索到的若干条记录压缩成“议题背景摘要”。
   - **议题状态**：
     - `project_profile`：记录当前版本的结构化信息（依据动态 schema）；
     - 支持字段归档与版本迁移（见后文）。

   > 说明：议题相关的历史记录/笔记/总结等，落库时统一复用 AI 工厂的 `entries` 主表与向量库设计：
   > - 节点的**语义角色/层级**由 `space_type` 和场景标签（如部门/规划/执行/议题/融助手等）表达；
   > - 节点之间的**父子关系**通过 `entry_id / parent_entry_id` 等 ID 字段表达；
   > - 即遵守《表结构设计25-12-8.md》中“标签作为语义层、ID 对作为父子关系物理层，由语义层驱动物理层”的统一约定，保证融助手生成的结构化议题卡片与全局数据树在同一语义/物理骨架上对齐。

3. **模型层（LLM Layer）**
   - 提供统一的模型调用接口 `call_llm(kind, messages, ...)`：
     - `kind = "dialog"` → 面向多轮对话 / 格式化输出的模型（测试期：DeepSeek-Chat；未来：本地对话模型）。
     - `kind = "reasoning"` → 面向结构规划 / 深度分析的推理模型（测试期：DeepSeek-Reasoning；未来：本地推理模型）。
   - 通过环境变量/配置可切换本地或远端模型，实现**统一抽象、可替换实现**。

### 2.2 逻辑多 Agent 角色

在工程上可以放在一个模块中，用函数/类区分角色；逻辑上有 6 个关键角色：

1. **T1：Topic Intent Analyzer（议题意图分析器，Reasoning）**
2. **T2：Topic Structure Planner（提问空间规划器，Reasoning）**
3. **S：Socratic Questioner（苏格拉底提问者，Dialog）**
4. **P：Project/Topic Profiler（议题卡片编辑器，Dialog）**
5. **M：Memory & Context Manager（记忆与上下文管理者，本地逻辑）**
6. **G：Generator（总结与文档生成者，Dialog/Reasoning）**

后文逐一说明其职责与交互。

---

## 3. 议题意图与提问空间（T1/T2）

### 3.1 关键原则

- **语义只是原始材料，真正决定结构的是“意图”。**
- 流程应严格拆成两步：
  1. **议题意图分析（T1）**：从原始描述中抽象出任务类型、视角、目标等 → 得到 `topic_intent`。
  2. **提问空间/结构规划（T2）**：仅基于 `topic_intent`（而非原始语句）生成 `topic_schema`。
- `topic_intent` 的本质：
  > 定义一个“提问空间”的维度与边界（提什么、从哪几个角度提、不提什么）。
- `topic_schema` 的本质：
  > 把这个空间具体化为 sections / fields / question_slots，可用于前端展示与后续苏格拉底提问。

### 3.2 T1：Topic Intent Analyzer（议题意图分析器）

**输入**：

- 议题标题、简要描述；
- 标签：至少包含 `议题`，以及其他业务标签（如“项目规划”“架构评审”等）；
- 可选：用户当前给出的简单期望（如“想为管理层决策做准备”）。

**输出：结构化 `topic_intent` 对象（示意）**：

```jsonc
{
  "intent_id": "intent-2025-01-01-001",
  "version": 1,
  "topic_kind": "technical_architecture_review",         // 议题类型
  "primary_perspective": "tech",                        // 主要视角
  "secondary_perspectives": ["product"],                // 次视角（可选）
  "goal": "prepare_decision",                           // 这次梳理的核心目的
  "focus": ["risks", "options_comparison"],            // 特别关注的维度
  "expected_output_style": "decision_memo",            // 期望最终输出形态
  "complexity_level": "medium",                        // 粗略深度/粒度
  "recommended_sections_meta": {                         // 对结构模块的建议草案
    "sections": [
      {"id": "context", "title": "背景与现状", "priority": "high"},
      {"id": "requirements", "title": "关键需求与约束", "priority": "high"},
      {"id": "options", "title": "候选方案", "priority": "medium"},
      {"id": "risks", "title": "风险与未决问题", "priority": "high"},
      {"id": "decision", "title": "决策与后续动作", "priority": "medium"}
    ]
  },
  "question_slots": [                                   // 提问空间中的“逻辑槽位”，而非最终问句
    {
      "id": "qs_context_main_problem",
      "section_hint": "context",
      "purpose": "搞清楚本议题要解决的核心问题是什么",
      "priority": "high"
    }
  ],
  "notes_for_planner": "用户强调要给管理层决策用，结构中必须包含决策建议板块。"
}
```

**特点与约束**：

- T1 **不输出面向用户的自然语言提问**；
- 它定义的是“本议题的任务类型与提问空间维度”（坐标系），供后续结构规划和提问使用；
- Reasoning 模型适合做这一步。

### 3.3 T2：Topic Structure Planner（提问空间规划器）

**输入**：

- 仅使用 `topic_intent`（不再重新解析原始语句）；
- 结合一组内置模板库（如：项目规划类、制度设计类、技术评审类、复盘类等）。

**输出：正式的 `topic_schema`（示意）**：

```jsonc
{
  "schema_id": "schema-2025-01-01-001",
  "intent_id": "intent-2025-01-01-001",
  "version": 1,
  "topic_kind": "technical_architecture_review",
  "sections": [
    {
      "id": "context",
      "title": "背景与现状",
      "order": 10,
      "fields": [
        {
          "id": "current_problem",
          "label": "当前主要问题",
          "required": true,
          "question_slots": ["qs_context_main_problem"]
        }
      ]
    },
    {
      "id": "requirements",
      "title": "关键需求与约束",
      "order": 20,
      "fields": [ ... ]
    },
    ...
  ]
}
```

**职责与边界**：

- T2 在一个“受控模板空间”内，为本议题**定制/裁剪结构**：
  - 不凭空发明全新结构，而是在已有模板上组合、调整、删减；
- T2 输出的 `topic_schema` 是：
  - UI 中“议题结构面板”的骨架；
  - S/P/G 等角色后续运行时的**提问空间边界定义**。

---

## 4. 会话与议题卡片结构（M / P）

### 4.1 会话结构 `SocraticSession`

```jsonc
{
  "session_id": "sess-2025-01-01-001",
  "project_code": "proj-rhon",          // 对应现有项目
  "user_id": "beibei",
  "tags": ["议题", "融助手"],
  "topic_title": "新一代项目背景梳理系统",
  "topic_intent": { ... },                // 当前意图版本
  "topic_schema": { ... },                // 当前结构（提问空间）版本
  "profile": { ... },                     // 见下文 ProjectProfile
  "messages": [                           // 会话历史（裁剪后的最近若干条）
    {"role": "system", "content": "...融助手角色设定..."},
    {"role": "user", "content": "我有一个新项目，大概是想..."},
    {"role": "assistant", "content": "好的，我们先从目标开始..."}
  ],
  "created_at": "...",
  "updated_at": "..."
}
```

### 4.2 议题卡片 `ProjectProfile`（schema 驱动）

`ProjectProfile` 不再是写死字段，而是被 `topic_schema` 驱动的 data 容器：

```jsonc
{
  "schema": { ...topic_schema_vN... },
  "data": {
    "context": {
      "current_problem": "...",
      "existing_system": "..."
    },
    "requirements": { ... },
    "risks": [ ... ],
    ...
  },
  "archived_fields": {                     // 由于意图/结构修改而“退役”的字段
    "v1_only": { ... }
  },
  "version": 3,
  "last_updated_at": "..."
}
```

### 4.3 P：Project/Topic Profiler（议题卡片编辑器）

**输入**：

- 当前用户回答文本；
- 当前 `topic_schema`；
- 当前 `ProjectProfile.data`。

**输出**：

- `profile_patch`（增量更新）：

```jsonc
{
  "updated_fields": {
    "context.current_problem": "...用户回答解析而来..."
  },
  "uncertain_points": [
    {
      "field_id": "requirements.non_functional",
      "reason": "用户提到性能和安全，但表述含糊，建议后续单独澄清。"
    }
  ]
}
```

**行为**：

- 将自然语言回答解析到对应 section/field；
- 标记不确定/模糊内容供后续苏格拉底提问继续追问。

### 4.4 M：Memory & Context Manager（记忆与上下文管理）

- 维护 `SocraticSession`：
  - 负责追加对话消息、裁剪历史、生成早期摘要；
  - 应用来自 P 的 `profile_patch` 更新 `ProjectProfile`。
- 调用 RAG：
  - 使用现有 `entries` + 向量库，按 `project_code` + `议题` (+ `融助手`) 检索；
  - 得到若干条历史记录，压缩为 `background_summary`；
- 为所有 LLM 调用构造统一的 `messages`：
  - system：融助手角色设定；
  - （可选）assistant：当前 `topic_intent` / `topic_schema` / `ProjectProfile` 的摘要；
  - 最近 N 条用户/助手对话；
  - 当前用户输入（若有）。

---

## 5. 苏格拉底提问与总结（S / G）

### 5.1 S：Socratic Questioner（苏格拉底提问者）

**输入**：

- 当前 `topic_schema`（提问空间）；
- 当前 `ProjectProfile.data` 填充状态；
- 最近对话历史 `messages`（含用户语气、已答/未答内容）。

**输出**：

- 给系统（结构化）：

```jsonc
{
  "target_fields": ["context.current_problem", "requirements.core"],
  "coverage_estimate": 0.65,            // 粗略覆盖度估计，用于辅助提示
  "missing_sections": ["risks", "decision"],
  "notes_for_user": "目前目标和背景已较清晰，尚未讨论风险与决策建议。"
}
```

- 给用户（自然语言）：
  - 1–2 个苏格拉底式问题（中文），严格基于上述 `target_fields` 和“提问空间”。

**行为规则**：

- 每轮只提出 1–2 个问题，避免问卷式轰炸；
- 优先补充：
  - `required = true` 且未填的字段；
  - `priority = high` 且信息明显不足的字段；
- 避免重复提问已回答过的内容；
- 周期性向用户报告当前“覆盖情况”和“尚未触及的模块”，并询问：
  - 是否继续挖掘这些模块；
  - 或是否先生成阶段性/最终总结。

### 5.2 G：Generator（总结与文档生成者）

**触发方式**：

- 用户显式请求：“先给我一版项目/议题背景总结”；
- 或 S 判断覆盖度较高后，向用户发起建议，用户确认。

**输入**：

- 完整的 `ProjectProfile`（基于当前 `topic_schema`）；
- 少量最近对话，用于把握语气与表达偏好。

**输出**：

- 一份结构化议题说明文（Markdown/文本）：
  - 分章节对应 `topic_schema.sections`；
  - 可附带“待补充/未决问题列表”。

**与 DB/向量库的集成**：

- G 的重要输出应写入 `entries`：
  - `project_code = 当前项目`；
  - 标签包含：`议题`、`融助手`；
  - 文本内容为当前总结/关键结论，供未来 RAG 使用。

---

## 6. 意图修改与接续提问（版本化提问空间）

这是本设计中特别重要、已充分讨论的场景。

### 6.1 可视化“议题结构/提问空间面板”

- 前端应提供一个**独立展示区域**，基于 `topic_intent + topic_schema` 呈现：
  - 议题意图摘要：
    - 类型（topic_kind）
    - 视角（primary/secondary_perspective）
    - 核心目标（goal）
    - 重点维度（focus）
  - 结构树：
    - sections（背景、目标、方案、风险、决策等）
    - 每个 section 的完成度状态：未触及 / 进行中 / 大致完成。
- 这是你可见且可编辑的**“提问空间控制面板”**。

### 6.2 允许在同一议题中途修改意图/结构

用户可以在面板上做两类改动：

1. **修改意图（上层）**
   - 如：
     - 把 goal 从“仅梳理现状”改为“为管理层决策准备材料”；
     - 把 primary_perspective 从 `tech` 改为 `product+tech`。

2. **修改结构（下层）**
   - 增、删、调序某些 sections 或 fields；
   - 标记某些板块为“本议题不关心，跳过”。

### 6.3 后端对修改的处理流程

设当前会话为 `session_id = S1`，原状态：

- `topic_intent_v1`
- `topic_schema_v2`
- `ProjectProfile.version = 2`（数据已部分填充）。

当用户修改意图/结构并确认后，后端执行：

1. **更新/重建 `topic_intent_v2`**
   - 合并用户修改后的字段；
   - 如需，可调用 T1 做一次一致性校验与补充。

2. **调用 T2，基于 `topic_intent_v2` 生成 `topic_schema_v3`**
   - 输入只看 `topic_intent_v2`；
   - 输出新的 sections/fields 结构（提问空间的新版本）。

3. **对比旧/新 schema，迁移和归档数据**

- 找出：
  - **保留字段**：旧/新 schema 都存在的字段 → 原有填充值保留；
  - **废弃字段**：只在旧 schema 中存在的字段 → 将对应数据移动到 `archived_fields`；
  - **新增字段**：只在新 schema 中出现的字段 → 初始化为空，标记为“待填充”。

4. **更新 `ProjectProfile`**

```jsonc
{
  "schema": { ...topic_schema_v3... },
  "data": { ...迁移后的当前数据... },
  "archived_fields": { ...v1/v2 中退役字段的数据... },
  "version": 3,
  "last_updated_at": "..."
}
```

5. **在会话历史中记录结构变更事件**

- 在 `messages` 或单独的 `events` 中加入一条系统说明：
  - 描述：
    - 用户修改了哪些意图/结构；
    - 哪些模块被保留/归档/新增；
    - 接下来苏格拉底提问会重点关注哪里。

### 6.4 修改后的苏格拉底提问行为

- S 在生成下一轮问题时：
  - 只使用最新的 `topic_schema_v3` 和迁移后的 `ProjectProfile.data`；
  - 优先针对“新增/未填/优先级高”的字段提问；
  - 不再对已归档字段继续追问。
- 在语气上，应对你有一个“结构变更后的状态确认”说明，例如：

> “根据你刚才调整的意图，我们将重点围绕：A/B/C 三块继续挖掘。  
>  之前在 D/E 板块中的内容已作为历史信息归档，不再作为本轮提问重点。  
>  接下来，我会先从 A 的关键问题问起，可以吗？”

**效果**：

- 你可以在**同一个议题下动态调整视角/目标/结构**；
- 揉助手不会推翻已收集信息，而是在新的提问空间内**自然接续**；
- 你仍可在任何时候“中断当前议题、另起新议题”，这是会话级控制。

---

## 7. 模型使用策略与安全/隐私

### 7.1 模型分工

- `call_llm(kind="dialog", ...)`：
  - 测试阶段：DeepSeek-Chat；
  - 未来：本地中文对话模型；
  - 用于：
    - S（提问者）
    - P（解析用户回答）
    - G（一般总结/说明生成）。

- `call_llm(kind="reasoning", ...)`：
  - 测试阶段：DeepSeek-Reasoning；
  - 未来：本地推理增强模型；
  - 用于：
    - T1（议题意图分析）
    - T2（提问空间/结构规划）
    - 必要时用于一次性深度复盘/矛盾分析。

### 7.2 隐私与本地优先

- 核心原则：
  - 长期目标是**以本地模型为主**，保护原始想法与议题内容；
  - 远端模型仅在配置允许且经过摘要/脱敏后使用（尤其是 Reasoning）。
- 设计上通过统一 `call_llm(kind=...)` 抽象，保证：
  - 切换远端/本地只需改配置，不影响上层编排逻辑；
  - 可以为“内容保护期”指定“只用本地模型”。

---

## 8. 决策权分配与交互原则

### 8.1 决策权：人主导，Agent 辅助

- 用户（议题主导者）：
  - 决定：
    - 何时启动/结束一个议题会话；
    - 何时修改意图/结构；
    - 何时生成阶段性/最终总结；
  - 可随时打断/纠偏：“这个问题不重要，换个角度”等。

- 融助手：
  - 基于提问空间和当前状态：
    - 设计一轮轮苏格拉底式提问；
    - 解析你的回答并维护议题卡片；
    - 监控覆盖情况、发现缺口，**提出“是否进入总结”的建议**；
  - 但**不能单方面宣布对话结束**，只提供评估与提醒。

### 8.2 提问空间与多轮问答的关系

- **意图分析（T1） = 定义提问空间与维度**；
- **结构规划（T2） = 把空间具体化为 sections/fields/slots**；
- **苏格拉底提问（S） = 在这个空间内沿着这些维度多轮探索提问**；
- 你可以在任何时刻重新定义空间（修改意图/结构），融助手在新空间内继续提问。

---

## 9. 与现有 ai-factory / 三栏 / RAG 的关系

- 三栏页面 + RAG 已经提供：
  - 工作记录入库（entries 表）；
  - 向量检索；
  - project_code + tag 维度的过滤。

在融助手设计中：

- 这些能力自然成为**中长期外部记忆（Long-term Memory）**：
  - 存放：历史议题总结、关键讨论片段、阶段性结论；
  - 标签组合：`project_code` + `议题` (+ `融助手`)。
- 短期对话上下文（多轮历史 + 当前提问空间）
  - 由 `SocraticSession` 结构和 M 角色管理；
  - 不和 RAG 混淆。

两者互补：

- RAG 负责“从历史知识中拉素材”；
- 会话上下文负责“跟踪当前这一次苏格拉底对话在走到哪一步”。

---

## 10. 实施建议与后续工作

1. **先按本设计文档在 ai-factory 内部实现：轻量 Orchestrator + 多角色函数**：
   - 不强制依赖 Autogen 或 LangChain 的多 Agent 框架；
   - 可在后续阶段把 S/P/M/G/T1/T2 封装为标准 Agent。

2. **优先实现的最小闭环**：
   - 基本流程：
     1. 校验标签包含 `议题`；
     2. T1 → `topic_intent`；
     3. T2 → `topic_schema` + 可视化面板；
     4. 启动 S + P + M 的多轮问答闭环；
     5. G 生成一期总结，并写入 entries（打上 `议题`、`融助手` 标签）。

3. **第二阶段**：
   - 支持“中途修改意图/结构 → 提问空间版本化 → 数据迁移与接续提问”。

4. **第三阶段**：
   - 深度引入 Reasoning 模型做：
     - 更复杂的议题结构规划；
     - 深度复盘/矛盾分析；
   - 根据使用体验，视情况挂接 Autogen Team 或 LangChain Workflow 做标准化编排。

---

> 本文档作为苏格拉底融助手的总体设计与需求对齐文档，后续如有调整，应在版本头部更新版本号与变更说明。
