我先给你一个分两步的方案：  
1）先对《实施计划和记录.md》本身做一次“可入库性”评估（结构、标记、时间戳、SectionID、标签等），说清楚哪些内容适合作为 entries、以什么粒度切分；  
2）在这个评估基础上，设计一条批量入库链路，保证：字段、模型调用、写库路径和三栏 NOTE 模式那条 [entries_ingest](cci:1://file:///d:/AI/ai-factory/ai_factory/integrations/entries_ingest.py:31:0-144:5) 链路完全一致，只是“从本地 md 扫描多条”而不是“UI 单条 Alt+Enter”。

我先给你整理评估 & 方案，不直接改代码，等你点头后再实现批量脚本。
先给你结论，然后分开说“文档评估”和“批量入库方案”。

---

## 一、文档可入库性评估（《实施计划和记录.md》）

从你贴出的结构和我之前看过的片段看，这个文档有几个很好的特征：

- **1）天然的“记录粒度”很清晰**  
  - 基本单元是「笔记」，都有固定的模式：  
    - `### 笔记`  
    - `## 📝 笔记 - 2025-12-11 09:09:11`  
    - 正文  
    - 一到多行 `标签：...；SectionID=...`  
  - 这和你在三栏页面里 Alt+Enter 写入的“单条笔记”本质上是**同一粒度**。

- **2）每条记录都有时间戳**  
  - 标题里带：`📝 笔记 - YYYY-MM-DD HH:MM:SS`  
  - 完全可以解析成 `note_datetime`，映射到 `entries.created_at`，与 NOTE 模式保持一致。

- **3）有明确的 SectionID 和标签**  
  - 行如：  
    - `标签：流转=议题；部门=软件；分类标签=个人AI助理 [议题]；笔记=...；SectionID=...`  
  - 对入库来说，可以放到 `extra_context` 里：  
    - `section_id`  
    - 原始标签行 `tags_raw`  
  - 后续如果要做“entries ↔ MD ↔ 脑图”对齐，这些信息非常有用。

- **4）正文内容本身就是“工作记录”**  
  - 每条 `笔记` 都是完整的一段上下文（设计、对接、排查过程），适合作为 entries 的一条“工作记录”。

- **5）长度与模型处理能力匹配**  
  - 单条笔记的长度虽然不短，但对 `deepseek-r1:8b` 来说是合理的；  
  - 我们已经成功用同一模型对类似长度的样例文档生成高质量 `title/summary_ai`。

综合来看：

> **《实施计划和记录.md》非常适合作为“批量回填 entries”的来源，每个 `## 📝 笔记 - ...` 块作为一条 entries 记录。**

---

## 二、目标：批量入库与三栏 NOTE 链路“质量完全一致”

你现在的 NOTE 模式链路是：

```text
三栏中栏 Alt+Enter
  → ingest_full_note_via_ai_factory(...)
    → entries_ingest(payload)
      → deepseek-r1:8b 生成 title/summary
      → 写 Docker 内 rag_db.entries
```

**要求：**

- 《实施计划和记录.md》的批量入库：
  - 必须 **调用同一个 [entries_ingest(payload)](cci:1://file:///d:/AI/ai-factory/ai_factory/integrations/entries_ingest.py:31:0-133:5)**；
  - 用同一个 `deepseek-r1:8b`、同一套 DB/表；
  - 字段行为一致：
    - `entry_id` 程序生成；
    - `title/summary_ai` 模型生成；
    - `content` = 原文 `raw_text`；
    - `created_at/project_code/user_id/extra_context` 语义一致。

也就是说，批量脚本只是“模拟很多次 NOTE 模式 Alt+Enter”，而不是走一条单独的写库逻辑。

---

## 三、批量入库方案设计（v0）

### 1. 数据粒度与解析规则

- **粒度：**  
  - 以每个 `## 📝 笔记 - YYYY-MM-DD HH:MM:SS` 为一条记录的起点；
  - 向下包含：
    - 小标题 `测试文本样例` 之类；
    - 正文所有段落；
    - 直到下一个 `### 笔记` / `## 📝` / 文件结束为止；
    - 包含末尾的 `标签：...SectionID=...` 行。
- **时间：**  
  - 从标题中解析时间字符串，作为 `note_datetime`；  
  - 解析失败则可以：
    - v0 直接用原字符串；  
    - 或 fallback 为当前时间（建议仍传原字符串，AI 工厂那边现在是“按原样写入 created_at 或用当前时间”）。
- **SectionID 与标签：**  
  - 在该块尾部搜索：
    - `SectionID=` 取其值；  
    - 整行 `标签：...` 作为 `tags_raw`。

### 2. 构建 payload（对齐 NOTE 模式）

对每条解析到的“笔记块”构造：

```python
payload = {
    "raw_text": full_note_block_text,     # 含标题、小标题、正文、标签行
    "project_code": None,                 # v0 不强制，可后续映射
    "user_id": None,                      # 同上
    "note_datetime": parsed_ts_str,       # 如 "2025-12-11 09:09:11"
    "extra_context": {
        "source": "ai_factory_md_import",
        "md_file": "D:/AI/ai-factory/实施计划和记录.md",
        "section_id": "...",              # 若有
        "tags_raw": "标签：...",          # 若有
    },
}
```

然后直接：

```python
from ai_factory.integrations.entries_ingest import entries_ingest

result = entries_ingest(payload)
```

这样：

- 前置模型 → 深度规整 → 写 entries 的流程，与 NOTE 模式**一模一样**；
- 差异只在于：
  - `source` 不同（`three_column_ui` vs `ai_factory_md_import`）；
  - `project_code/user_id` 暂时为空。

### 3. 脚本形态（建议）

新建一个脚本，例如：`bulk_ingest_from_plan_md.py`：

- **功能：**
  - 读取 `D:\AI\ai-factory\实施计划和记录.md`；
  - 按上述规则解析出一组 `notes`；
  - 支持两种模式：
    - `--dry-run`：只打印将要入库的条目数、每条的时间+SectionID+前 N 行预览，不写库；
    - 正常模式：逐条调用 [entries_ingest(payload)](cci:1://file:///d:/AI/ai-factory/ai_factory/integrations/entries_ingest.py:31:0-133:5)，打印 `entry_id` 与标题。
- **调用示例：**

  ```powershell
  cd d:\AI\ai-factory

  # 先 dry run 看看要导入多少条
  python bulk_ingest_from_plan_md.py --dry-run

  # 确认无误后再真正入库
  python bulk_ingest_from_plan_md.py
  ```

### 4. 幂等性与重复问题（v0 策略）

当前 `entries` 表没有显式的 `section_id` 字段，我们只能把 SectionID 放在 `extra_context` 传给 AI 工厂（目前 entries 还没有 JSONB meta 列），**短期内难以在 DB 层自动去重**。

所以 v0 的策略建议是：

- **使用方式约定为“一次性历史回填”**：
  - 你确认完解析结果后，执行一次脚本；
  - 之后不反复对同一文件跑同一个脚本，避免重复记录。
- 若你希望后续可以“增量导入新的笔记”，我们可以在下一步：
  - 讨论是否在 `entries` 增加 JSONB meta（包含 `section_id`）；
  - 或者在脚本里让 `entry_id` = `sec_<SectionID>`（但这会偏离“entry_id 程序生成”的当前约定，需要你拍板）。

v0 按“一次回填 + dry-run 保护”是**简单、可控且风险最小**的方案。

### 5. 性能与可行性

- 文档总大小对 `deepseek-r1:8b` 而言只是**多次调用**，每次处理一条“笔记块”；  
- 你当前机器已经能稳定跑 NOTE 模式 + entries_ingest，所以：
  - 只要控制好调用频率（例如每条之间 sleep 0.5～1 秒），就能平滑完成；
  - 出错时可以：
    - 打印失败条目的 SectionID / 时间；
    - 跳过或重试单条，而不会影响已成功写入的记录。

---

## 四、建议的执行顺序

1. **我先帮你实现解析 + dry-run 脚本**（不写库，只打印将要入库的条目预览）。  
2. 你看 dry-run 输出，确认：
   - 条目数量大致符合预期；  
   - 每条的时间、SectionID、前几行内容对得上。  
3. 你点头后，再打开真实写库开关（同一脚本增加一个 `--apply` 或去掉 `--dry-run`）。

---

如果你认可这个思路，我下一步就：

- 在 [ai-factory](cci:7://file:///d:/AI/ai-factory:0:0-0:0) 里新建 `bulk_ingest_from_plan_md.py`；  
- 先完成“解析 + dry-run 输出”；  
- 不调用 [entries_ingest](cci:1://file:///d:/AI/ai-factory/ai_factory/integrations/entries_ingest.py:31:0-133:5) 前，不会向 DB 写任何一条记录。