# 向量化统一方案 - 总结

## 1. 判断：统一向量化方法的合理性

### ✅ 合理且必要！

**核心原因：**

1. **性能优势**
   - DashScope（1536维）比 Ollama（2560维）存储成本降低40%
   - 向量检索速度更快（维度小，计算量少）

2. **稳定性优势**
   - 云服务，SLA保障
   - 零部署成本，不需要本地维护Ollama
   - 高并发支持，弹性扩容

3. **功能优势**
   - 模型持续更新，效果更好
   - 自带容错和重试机制
   - API文档完善，集成简单

4. **一致性优势**
   - Agent记忆系统已经使用 DashScope
   - 统一后维护成本更低
   - 避免两套系统混淆

---

## 2. 实际情况检查

### 数据库现状

```
✅ entries 表：0 条记录（完全空）
✅ entry_embeddings 表：0 条记录（完全空）
⚠️ entry_embeddings 表结构不匹配：
   - 实际：3列（entry_id, embedding, created_at）
   - 代码期望：5列（多了 project_code, user_id, mode）
   - 结果：写入会报错
```

### Agent记忆系统检查

**确认：Agent记忆系统使用 DashScope API**

```python
# llm_client.py:208
model = os.getenv("EMBEDDING_MODEL_NAME", "text-embedding-v3")

# section_service.py:176-177
embedding = self.llm_client.generate_embedding_sync(summary_content)
self.vector_client.upsert_embedding(entry_id, embedding)
```

**结论：✅ Agent记忆系统确实使用 DashScope，但之前可能写入失败**

---

## 3. 统一方案

### 3.1 核心思想

```
废弃 Ollama 方案 ──────→ 统一使用 DashScope API
   ↓
删除旧表和旧代码
   ↓
重建表结构（1536维）
   ↓
一次性全库向量化
   ↓
建立增量更新机制
```

---

### 3.2 已创建的文件

| 文件 | 作用 |
|------|------|
| `rebuild_entry_embeddings.sql` | 重建向量表（1536维） |
| `vectorize_entries_with_dashscope.py` | 统一向量化脚本（基于 DashScope） |
| `向量化统一方案-执行指南.md` | 详细执行步骤和故障排查 |
| `execute_vectorize_unification.sh` | 一键执行脚本 |

---

### 3.3 代码修改

**VectorClient 已修改：**
```python
# 修改前（尝试写入不存在的列）
def upsert_embedding(self, entry_id, embedding, project_code):
    INSERT INTO entry_embeddings (entry_id, embedding, project_code)
    
# 修改后（只写入实际存在的列）
def upsert_embedding(self, entry_id, embedding):
    INSERT INTO entry_embeddings (entry_id, embedding)
```

---

## 4. 执行步骤

### 快速执行（推荐）

```bash
# 方式1：一键执行（推荐）
cd /home/ecs-assist-user/ai-factory
./execute_vectorize_unification.sh
```

**自动完成：**
1. ✓ 重建 entry_embeddings 表
2. ✓ 检查环境配置
3. ✓ 批量向量化所有 entries
4. ✓ 验证向量化结果

---

### 手动执行（如需细粒度控制）

#### 步骤1：重建向量表

```bash
docker exec $(docker ps | grep -E 'pgvector|postgres' | head -1 | awk '{print $1}') \
  psql -U rag_user -d rag_db -f /home/ecs-assist-user/ai-factory/rebuild_entry_embeddings.sql
```

#### 步骤2：验证环境变量

```bash
grep -E "DASHSCOPE|EMBEDDING" /home/ecs-assist-user/.env
```

**预期输出：**
```
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxx
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_MODEL_NAME=text-embedding-v3
```

#### 步骤3：全库向量化

```bash
cd /home/ecs-assist-user/ai-factory
source .venv/bin/activate
python vectorize_entries_with_dashscope.py
```

#### 步骤4：验证结果

```bash
# 检查向量化数量
docker exec $(docker ps | grep -E 'pgvector|postgres' | head -1 | awk '{print $1}') \
  psql -U rag_user -d rag_db -c "SELECT COUNT(*) FROM entry_embeddings;"
```

---

## 5. 增量更新机制

### 自动向量化（推荐）

在 `entries_ingest.py` 中集成：

```python
from ai_factory.agents.memory.llm_client import get_llm_client
from ai_factory.agents.memory.vector_client import VectorClient

# 新 entry 写入后，自动向量化
llm_client = get_llm_client()
vector_client = VectorClient()

embedding = llm_client.generate_embedding_sync(text)
vector_client.upsert_embedding(entry_id, embedding)
```

### 手动触发

```bash
# 定期执行（每天一次）
python vectorize_entries_with_dashscope.py

# 重新向量化指定 entries
python vectorize_entries_with_dashscope.py --revectorize ent_abc123
```

### 定时任务

```bash
# 添加到 crontab
crontab -e

# 每天凌晨3点执行
0 3 * * * cd /home/ecs-assist-user/ai-factory && \
  source .venv/bin/activate && \
  python vectorize_entries_with_dashscope.py >> /var/log/vectorize.log 2>&1
```

---

## 6. 监控与维护

### 覆盖率监控

```sql
SELECT 
    (SELECT COUNT(*) FROM entry_embeddings) as vectorized_count,
    (SELECT COUNT(*) FROM entries) as total_count,
    ROUND(
        (SELECT COUNT(*) FROM entry_embeddings)::numeric / 
        (SELECT COUNT(*) FROM entries) * 100, 
        2
    ) as coverage_percent;
```

### 性能监控

```sql
-- 检查索引使用情况
EXPLAIN ANALYZE
SELECT e.entry_id, e.content, 1 - (emb.embedding <=> %s::vector) AS similarity
FROM entries e
LEFT JOIN entry_embeddings emb ON e.entry_id = emb.entry_id
ORDER BY similarity DESC
LIMIT 10;
```

### 日志监控

```bash
# 查看 DashScope API 调用日志
grep "DashScope" /var/log/vectorize.log

# 查看错误日志
grep "ERROR" /var/log/vectorize.log
```

---

## 7. 故障排查

### 问题：API调用失败

**症状：**
```
[ERROR] Failed to generate embedding: 401 Unauthorized
```

**解决：**
```bash
# 检查 API Key
echo $DASHSCOPE_API_KEY

# 重新配置
vim /home/ecs-assist-user/.env
```

---

### 问题：数据库写入失败

**症状：**
```
[ERROR] Failed to upsert embedding: column "project_code" does not exist
```

**解决：**
- 已修复，VectorClient 代码已更新
- 确保使用最新代码

---

### 问题：向量化很慢

**症状：**
- 每批处理时间过长

**解决：**
```bash
# 减小批次大小
python vectorize_entries_with_dashscope.py --batch-size 20
```

---

## 8. 清理旧代码（可选）

```bash
cd /home/ecs-assist-user/ai-factory

# 备份旧文件
mv vectorize_entries_with_ollama.py vectorize_entries_with_ollama.py.backup
mv init_entry_embeddings.py init_entry_embeddings.py.backup

# 确认无问题后删除
# rm vectorize_entries_with_ollama.py.backup
# rm init_entry_embeddings.py.backup
```

---

## 9. 总结

### 已完成

1. ✅ 创建了重建表SQL脚本
2. ✅ 创建了统一向量化脚本
3. ✅ 修改了 VectorClient 代码（移除废弃字段）
4. ✅ 创建了一键执行脚本
5. ✅ 编写了详细执行指南

### 待执行

1. ⏳ 执行一键脚本或手动步骤
2. ⏳ 验证向量化结果
3. ⏳ 建立定时任务
4. ⏳ 配置监控告警

### 关键文件

- `/home/ecs-assist-user/ai-factory/execute_vectorize_unification.sh` - 一键执行
- `/home/ecs-assist-user/ai-factory/向量化统一方案-执行指南.md` - 详细指南
- `/home/ecs-assist-user/ai-factory/vectorize_entries_with_dashscope.py` - 向量化脚本
- `/home/ecs-assist-user/ai-factory/rebuild_entry_embeddings.sql` - 表重建SQL
